name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  # The whole suite against a live server, on every Laravel the package declares: a search of the
  # driver goes to ManticoreSearch over the MySQL protocol, and without a server behind it most of
  # the suite would only be skipped.
  #
  # The matrix runs the floor of the range (Laravel 11 on PHP 8.2), each major above it, and the
  # newest PHP the framework allows (8.5), so both ends of what composer.json promises are checked
  # rather than assumed.
  #
  # The last two jobs are about the other end of the driver: the server. What Manticore answers
  # with differs between its versions - the column of SHOW TABLES, the wording of a missing table,
  # whether a write to a table that is not there creates one - and the two older images say
  # whether the range this package claims is a range it has run on.
  tests:
    name: tests / PHP ${{ matrix.php }}, Laravel ${{ matrix.laravel }}, Manticore ${{ matrix.manticore }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - php: '8.2'
            laravel: '11'
            testbench: '^9.0'
            manticore: 'latest'
            eol: true
          - php: '8.3'
            laravel: '12'
            testbench: '^10.0'
            manticore: 'latest'
          - php: '8.4'
            laravel: '13'
            testbench: '^11.0'
            manticore: 'latest'
          - php: '8.5'
            laravel: '13'
            testbench: '^11.0'
            manticore: 'latest'
          # the oldest image the registry still carries, and one in the middle of the way here
          - php: '8.4'
            laravel: '13'
            testbench: '^11.0'
            manticore: '7.0.0'
          - php: '8.4'
            laravel: '13'
            testbench: '^11.0'
            manticore: '13.11.1'

    services:
      manticore:
        image: manticoresearch/manticore:${{ matrix.manticore }}
        ports:
          # the port the tests expect by default, see MANTICORE_TEST_HOST/MANTICORE_TEST_PORT
          # in phpunit.xml.dist
          - 9306:9306

    steps:
      - uses: actions/checkout@v5

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: pdo, pdo_mysql, mbstring, json
          coverage: none

      - name: Cache the composer files
        uses: actions/cache@v6
        with:
          path: ~/.cache/composer/files
          key: composer-${{ matrix.php }}-${{ matrix.testbench }}-${{ hashFiles('composer.json') }}
          restore-keys: composer-${{ matrix.php }}-${{ matrix.testbench }}-

      # Laravel 11 reached the end of its security support on March 12th, 2026, so every release
      # of it is flagged by an advisory and composer refuses to install one from scratch. It is
      # the floor of the package all the same: an application already running it installs this
      # package without a word, because composer never re-resolves what is already there. Testing
      # that floor is the one case that does ask for the framework from scratch, hence the flag -
      # a dev-only dependency, in a runner thrown away at the end of the job.
      - name: Allow the EOL framework of this job
        if: matrix.eol
        run: composer config audit.block-insecure false

      # the testbench pins the Laravel of the job; update, not install, because a library ships
      # no composer.lock and the current dependencies are what has to be checked
      - name: Install the dependencies
        run: |
          composer require --dev --no-update --no-interaction "orchestra/testbench:${{ matrix.testbench }}"
          composer update --prefer-dist --no-progress --no-interaction

      # the image promises no mysql or curl of its own, so the port is waited for from the
      # outside rather than through a health check of the container
      - name: Wait for Manticore
        run: |
          for i in $(seq 1 60); do
            (echo > /dev/tcp/127.0.0.1/9306) >/dev/null 2>&1 && exit 0
            sleep 1
          done
          echo 'Manticore did not answer on 127.0.0.1:9306'
          exit 1

      - name: Run the tests
        run: vendor/bin/phpunit

  # The static analysis and the style, on the floor of the range - what passes there passes above
  # it as well, and neither of the two needs a server.
  analysis:
    name: static analysis
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_mysql, mbstring, json
          coverage: none

      - name: Install the dependencies
        run: composer update --prefer-dist --no-progress --no-interaction

      - name: Analyse the sources
        run: vendor/bin/phpstan analyse --no-progress

  style:
    name: style
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      # the fixer is a tool of the job rather than a dependency of the package: its own
      # requirements would hold back the Symfony the framework of a test brings with it
      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          coverage: none
          tools: php-cs-fixer

      - name: Check the style of the sources
        run: php-cs-fixer fix --dry-run --diff --show-progress=none
